# Analytics Configuration Example
# Copy this to .streamlit/secrets.toml and fill in your keys.
# DO NOT commit secrets.toml to git — it is listed in .gitignore.

[analytics]
# Option 1: Google Analytics (free, full-featured)
# Get your Measurement ID from: https://analytics.google.com/
# google_analytics_id = "G-XXXXXXXXXX"

# Option 2: Plausible (privacy-focused, ~$9/month)
# Add your domain at: https://plausible.io/
# plausible_domain = "your-app.streamlit.app"

# Option 3: Simple logging (free, most private — always enabled as fallback)
# No configuration needed. Logs appear in:
#   Streamlit Cloud → your app → Manage app → Logs
# Search for lines starting with [ANALYTICS]
